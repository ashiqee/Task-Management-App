import React from 'react';
import './style.css'

const Button = ({ text }) => {
    return (
        <div className="">
            <button className="text-xl btn react">{text}</button>
        </div>
    );
};

export default Button;